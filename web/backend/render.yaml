services:
  - type: web
    name: regimeflow-web
    runtime: python
    buildCommand: pip install -r requirements.txt
    startCommand: python server.py
    envVars:
      - key: LOAD_CHRONOS
        value: "false"
      - key: PORT
        value: "8000"
